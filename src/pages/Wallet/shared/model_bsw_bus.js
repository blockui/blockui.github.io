export class ModelBswBus {
  constructor({
                tokenId, capacity, level, createTimestamp, uri
              }) {
    this.tokenId = tokenId
    this.capacity = capacity
    this.level = level
    this.createTimestamp = createTimestamp
    this.uri = uri
  }
}

